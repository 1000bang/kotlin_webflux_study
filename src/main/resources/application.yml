server:
  port: 8080

spring:
  application:
    name: everydoc-api

  # R2DBC (Step 18) — 논블로킹 DB 연결
  r2dbc:
    url: r2dbc:postgresql://106.240.247.44:5432/postgres
    username: postgres
    password: kpoint01!

  # JDBC datasource (참고용 — R2DBC 사용 시 불필요)
  # datasource:
  #   url: jdbc:postgresql://106.240.247.44:5432/postgres
  #   driver-class-name: org.postgresql.Driver
  #   username: postgres
  #   password: kpoint01!
  # sql:
  #   init:
  #     mode: never
